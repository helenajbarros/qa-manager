# Copie este arquivo para ".env" (na raiz do projeto, ao lado do docker-compose.yml)
# e troque os valores abaixo antes de subir o ambiente com Docker.
POSTGRES_USER=qa_manager
POSTGRES_PASSWORD=troque_esta_senha
POSTGRES_DB=qa_manager
JWT_SECRET=troque_este_segredo_por_algo_aleatorio_e_unico
